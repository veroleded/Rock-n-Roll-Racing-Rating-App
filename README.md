# Discord Bot & Web App — Rock n' Roll Racing

Полнофункциональная система для организации матчей, ведения рейтинга и статистики по игре **Rock n' Roll Racing**. Проект включает Discord-бота с командами управления и современное веб-приложение с админ-панелью.

> **Rock n' Roll Racing** — культовая аркадная гоночная игра от Blizzard (SNES/SEGA, 1993). Подробнее: [Wikipedia](https://en.wikipedia.org/wiki/Rock_n%27_Roll_Racing)

---

## 📋 Содержание

- [Основные возможности](#основные-возможности)
- [Технологии](#технологии)
- [Быстрый старт](#быстрый-старт)
- [Установка и настройка](#установка-и-настройка)
- [Инструкция для пользователей](#инструкция-для-пользователей)
- [Команды Discord-бота](#команды-discord-бота)
- [Веб-интерфейс](#веб-интерфейс)
- [Структура проекта](#структура-проекта)
- [Разработка](#разработка)
- [Деплой](#деплой)

---

## ✨ Основные возможности

### Discord-бот
- ✅ Автоматическое управление очередями на матчи (2x2, 3x3, 2x2x2)
- ✅ Автоматический сбор команд с ботами
- ✅ Просмотр статистики и рейтинга прямо в Discord
- ✅ Топ-10 и антитоп-10 игроков
- ✅ Уведомления о новых матчах

### Веб-приложение
- ✅ Авторизация через Discord OAuth
- ✅ Личный профиль с детальной статистикой
- ✅ Просмотр истории матчей с фильтрами
- ✅ Детальная статистика по каждому матчу (урон, деньги, дивизионы)
- ✅ Матрица урона между игроками
- ✅ Админ-панель для управления пользователями и матчами
- ✅ Система ролей (ADMIN, MODERATOR, PLAYER)
- ✅ Темная/светлая тема

---

## 🛠 Технологии

### Frontend
- **Next.js 15** (App Router, SSR)
- **TypeScript**
- **Tailwind CSS** + **shadcn/ui**
- **tRPC** для type-safe API
- **React Hook Form** + **Zod** для валидации
- **Recharts** для графиков

### Backend
- **tRPC** (type-safe API)
- **Prisma ORM** + **PostgreSQL**
- **NextAuth.js** (Discord OAuth)

### Bot
- **discord.js v14**
- **TypeScript**

### DevOps
- **Docker** + **docker-compose**
- **Nginx** (reverse proxy)

---

## 🚀 Быстрый старт

### Требования
- Node.js 18+ и npm
- PostgreSQL 14+
- Discord Bot Token и OAuth credentials

### 1. Клонирование репозитория

```bash
git clone <repo_url>
cd discord-bot-new
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DATABASE_URL="postgresql://user:password@localhost:5432/rnr_racing_db?schema=public"

# Discord OAuth (для NextAuth)
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_random_secret_key

# Discord Bot
DISCORD_BOT_TOKEN=your_discord_bot_token
DISCORD_GUILD_ID=your_discord_guild_id
DISCORD_ADMIN_IDS="discord_id_1,discord_id_2"  # ID администраторов через запятую
BOT_SECRET_KEY=your_secret_key_for_hmac  # Для подписи запросов от бота

# Приложение
APP_URL=http://localhost:3000
NODE_ENV=development

# PostgreSQL (для Docker)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=rnr_racing_db
POSTGRES_PORT=5432

# pgAdmin (опционально)
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
```

### 4. Настройка базы данных

```bash
# Применить миграции
npx prisma migrate deploy

# Заполнить тестовые данные
npm run seed

# Инициализировать ботов
npm run init-bots
```

### 5. Запуск

**Локально:**

```bash
# Терминал 1: Веб-приложение
npm run dev

# Терминал 2: Discord-бот
npm run bot
```

**Через Docker:**

```bash
docker-compose up --build
```

Приложение будет доступно на `http://localhost:3000`

---

## 👥 Инструкция для пользователей

### Первый запуск

1. **Присоединитесь к боту в Discord:**
   - Найдите бота на сервере
   - Отправьте команду: `!join`
   - Бот ответит ссылкой на веб-приложение

2. **Войдите в веб-приложение:**
   - Перейдите по ссылке из ответа бота (или откройте `http://localhost:3000`)
   - Нажмите "Войти через Discord"
   - Разрешите доступ приложению

3. **Готово!** Теперь вы можете использовать все функции системы.

### Использование очередей

Очереди работают только в специальных каналах сбора на игру.

**Основные команды:**
- `++` — Встать в очередь на матч
- `--` — Покинуть очередь
- `+bot` — Добавить бота в очередь
- `-bot` — Убрать бота из очереди
- `clean` — Очистить очередь (только для администраторов)

**Как это работает:**
1. Войдите в канал сбора на игру
2. Отправьте `++` чтобы встать в очередь
3. Когда наберется нужное количество игроков, бот автоматически соберет команды и создаст матч
4. Боты автоматически добавляются, если не хватает игроков

### Просмотр статистики

**В Discord:**
- `!stat` — Ваша статистика
- `!stat @пользователь` — Статистика другого игрока
- `!rank` — Ваша позиция в рейтинге и соседи
- `!rank @пользователь` — Позиция другого игрока
- `!top` — Топ-10 игроков
- `!bottom` — Антитоп-10 игроков
- `!help` — Список всех команд

**В веб-приложении:**
- Откройте **"Мой профиль"** для просмотра своей статистики
- Откройте **"Игроки"** для просмотра таблицы всех игроков
- Кликните на любого игрока для просмотра детальной статистики

### Просмотр матчей

1. Перейдите в раздел **"Матчи"**
2. Используйте фильтры:
   - По игроку
   - По режиму игры (2x2, 3x3, 2x2x2)
   - Только рейтинговые матчи
3. Кликните на любой матч для просмотра детальной статистики:
   - Статистика каждого игрока
   - Матрица урона между игроками
   - Графики урона, денег, аптечек
   - Статистика по дивизионам

### Добавление матча (для администраторов и модераторов)

1. Перейдите в **"Матчи"** → **"Добавить матч"**
2. Заполните форму:
   - Выберите режим игры
   - Выберите игроков для каждой команды
   - Загрузите JSON файл со статистикой матча
   - Укажите коэффициент штрафа (если нужно)
   - Отметьте, если это тренировочный матч
3. Нажмите **"Создать матч"**

**Формат JSON файла статистики:**
```json
{
  "damage": { "player_id": { "target_id": damage_value } },
  "scores": { "player_id": score },
  "mines_damage": { "player_id": damage },
  "money_taken": { "player_id": amount },
  "armor_taken": { "player_id": amount },
  "wipeouts": { "player_id": count },
  "total_score": { "team": score },
  "divisions": { "player_id": { "division_name": stats } }
}
```

Примеры файлов находятся в папке `test-data/`.

---

## 🤖 Команды Discord-бота

### Основные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `!join` | Присоединиться к боту (обязательно для входа в веб) | `!join` |
| `!stat [@user]` | Показать статистику игрока | `!stat` или `!stat @username` |
| `!rank [@user]` | Показать позицию в рейтинге и соседей | `!rank` или `!rank @username` |
| `!top` | Показать топ-10 игроков | `!top` |
| `!bottom` | Показать антитоп-10 игроков | `!bottom` |
| `!help` | Показать список всех команд | `!help` |

### Команды очередей (только в каналах сбора)

| Команда | Описание | Пример |
|---------|----------|--------|
| `++` | Встать в очередь на матч | `++` |
| `--` | Покинуть очередь | `--` |
| `+bot` | Добавить бота в очередь | `+bot` |
| `-bot` | Убрать бота из очереди | `-bot` |
| `clean` | Очистить очередь (только админы) | `clean` |
| `help` | Показать команды очереди | `help` |

---

## 🌐 Веб-интерфейс

### Основные разделы

#### 📊 Мой профиль (`/dashboard`)
- Личная информация
- Общая статистика (рейтинг, игры, винрейт)
- Детальная статистика по матчам и дивизионам
- История ваших матчей

#### 👥 Игроки (`/users`)
- Таблица всех игроков с сортировкой
- Фильтрация по рейтингу, играм, винрейту
- Просмотр профиля любого игрока

#### 🎮 Матчи (`/matches`)
- Список всех матчей с фильтрами
- Поиск по игроку, режиму, рейтинговости
- Детальная статистика каждого матча:
  - Статистика игроков
  - Матрица урона
  - Графики и визуализации
  - Статистика по дивизионам

#### ➕ Добавить матч (`/matches/add`)
- Форма создания нового матча
- Выбор игроков и ботов
- Загрузка JSON файла статистики
- Доступно для ADMIN и MODERATOR

#### ⚙️ Управление пользователями (`/users/[id]/edit`)
- Редактирование роли пользователя
- Редактирование статистики
- Удаление пользователя
- Доступно только для ADMIN

### Роли и права доступа

| Роль | Права |
|------|-------|
| **PLAYER** | Просмотр профиля, статистики, матчей |
| **MODERATOR** | Все права PLAYER + создание/редактирование/удаление последнего матча |
| **ADMIN** | Все права MODERATOR + управление пользователями, редактирование любого матча |

---

## 📁 Структура проекта

```
discord-bot-new/
├── src/
│   ├── app/                    # Next.js приложение
│   │   ├── (auth)/             # Страницы авторизации
│   │   ├── (protected)/        # Защищенные страницы
│   │   │   ├── dashboard/      # Личный профиль
│   │   │   ├── users/          # Управление пользователями
│   │   │   └── matches/        # Матчи
│   │   ├── api/                # API routes
│   │   │   ├── auth/          # NextAuth
│   │   │   └── trpc/          # tRPC endpoint
│   │   └── join-bot/           # Страница присоединения к боту
│   ├── bot/                    # Discord-бот
│   │   ├── commands/           # Команды бота
│   │   ├── services/           # Сервисы бота
│   │   ├── constants/          # Константы
│   │   └── utils/              # Утилиты бота
│   ├── components/             # React компоненты
│   │   ├── matches/            # Компоненты матчей
│   │   └── ui/                 # UI компоненты (shadcn/ui)
│   ├── server/                 # Backend логика
│   │   ├── routers/            # tRPC роутеры
│   │   ├── services/           # Бизнес-логика
│   │   └── utils/              # Утилиты сервера
│   ├── lib/                    # Общие утилиты
│   └── utils/                  # Клиентские утилиты
├── prisma/                     # Prisma
│   ├── schema.prisma           # Схема БД
│   ├── migrations/             # Миграции
│   ├── seed.ts                 # Сиды
│   ├── init-bots.ts            # Инициализация ботов
│   └── clean.ts                # Очистка БД
├── test-data/                  # Тестовые JSON файлы
├── docker-compose.yml          # Docker Compose (dev)
├── docker-compose.prod.yml     # Docker Compose (prod)
├── Dockerfile                  # Docker образ
├── nginx.conf                  # Nginx конфиг (prod)
├── nginx.dev.conf              # Nginx конфиг (dev)
└── package.json
```

---

## 🔧 Разработка

### Доступные команды

```bash
# Разработка
npm run dev          # Запустить Next.js в режиме разработки
npm run bot          # Запустить Discord-бота в watch режиме

# Сборка
npm run build        # Собрать production версию
npm start            # Запустить production версию

# База данных
npx prisma migrate dev    # Создать новую миграцию
npx prisma migrate deploy # Применить миграции
npx prisma studio         # Открыть Prisma Studio

# Данные
npm run seed         # Заполнить тестовыми данными
npm run init-bots    # Инициализировать ботов
npm run delete-bots  # Удалить ботов
npm run clean        # Очистить БД

# Качество кода
npm run lint         # Проверить код линтером
npm run format       # Форматировать код
npm run format:check # Проверить форматирование
```

### Работа с базой данных

**Prisma Studio** (GUI для БД):
```bash
npx prisma studio
```

**Создание миграции:**
```bash
# После изменения schema.prisma
npx prisma migrate dev --name migration_name
```

**Применение миграций:**
```bash
npx prisma migrate deploy
```

### Структура базы данных

Основные модели:
- `User` — пользователи (игроки и боты)
- `Stats` — статистика пользователя
- `Match` — матчи
- `MatchPlayer` — игроки в матче
- `Queue` — очереди на матчи

Подробнее в `prisma/schema.prisma`.

---

## 🚢 Деплой

### Docker Compose (Production)

1. Настройте `.env` файл с production значениями
2. Запустите:

```bash
docker-compose -f docker-compose.prod.yml up --build -d
```

### Ручной деплой

1. Соберите приложение:
```bash
npm run build
```

2. Запустите production сервер:
```bash
NODE_ENV=production npm start
```

3. Запустите бота:
```bash
NODE_ENV=production npm run bot
```

### Переменные окружения для production

Убедитесь, что установлены:
- `NODE_ENV=production`
- `APP_URL` — публичный URL приложения
- `NEXTAUTH_URL` — должен совпадать с `APP_URL`
- Все секретные ключи должны быть надежными

---

## 📝 Дополнительная информация

### Безопасность

- Все запросы от бота к серверу подписываются HMAC
- NextAuth для безопасной авторизации
- Middleware для защиты маршрутов
- Валидация данных через Zod

### Производительность

- Мемоизация компонентов React
- Оптимизация tRPC запросов (staleTime, refetchOnWindowFocus)
- Индексы в базе данных
- Кэширование на уровне Nginx

### Поддержка

При возникновении проблем:
1. Проверьте логи бота и сервера
2. Убедитесь, что все переменные окружения установлены
3. Проверьте подключение к базе данных
4. Убедитесь, что бот имеет необходимые права на сервере Discord

---

## 📄 Лицензия

[Укажите лицензию проекта]

---

## 👨‍💻 Авторы

Veroled

---

**Приятной игры в Rock n' Roll Racing! 🏎️💨**
